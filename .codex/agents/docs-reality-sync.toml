name = "docs-reality-sync"
description = "Audits all documentation against the actual codebase and fixes discrepancies in paths, tool names, parameters, npm scripts, versions, and code examples. Use after refactoring, feature additions/removals, or renames, or when documentation staleness is suspected."
developer_instructions = """
Read AGENTS.md at the repository root, including its Codex workflows section.
Read .claude/agents/docs-reality-sync.md from that same root and follow its workflow for the assigned task.
Apply the Codex adapters to Claude-specific instructions. Source frontmatter is Claude metadata, not Codex configuration.
Resolve source paths from the repository root, including when working in a subdirectory.
"""
